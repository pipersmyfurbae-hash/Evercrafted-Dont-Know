## Supabase — project "Final EcoSystem" (kxkvsrwpezusqvriftqv)
# URL and anon/publishable key are not secret — safe to commit a real value,
# but keep using env vars so other environments can point elsewhere.
VITE_SUPABASE_URL=https://kxkvsrwpezusqvriftqv.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_jT7UCGSoRXIAkAh73DZoSg_vjbVbc_5

## Server-only — bypasses Row Level Security entirely. NEVER commit a real
## value, never expose to the browser. Get it from Supabase dashboard ->
## Project Settings -> API -> service_role secret.
SUPABASE_SERVICE_ROLE_KEY=

## Gemini / Google GenAI
GEMINI_API_KEY=
